import { YoutubeExtractIDPipe } from './youtube-extract-id.pipe';

describe('YoutubeExtractIDPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeExtractIDPipe();
    expect(pipe).toBeTruthy();
  });
});
